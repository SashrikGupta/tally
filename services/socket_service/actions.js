// Mirrors client/src/lib/constants.js ACTIONS exactly — keep both in sync.
const ACTIONS = {
  JOIN: 'join',
  JOINED: 'joined',
  DISCONNECTED: 'disconnected',
  CODE_CHANGE: 'code-change',
  SYNC_CHANGE: 'sync-code',
  CHAT: 'chat',
  LEAVE: 'leave',
};

module.exports = ACTIONS;
